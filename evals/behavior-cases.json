[
  {
    "id": "performance-no-artifacts",
    "skill": "spring-performance-investigator",
    "artifact_mode": "none",
    "prompt": "Our p99 doubled after a release. We only know server.tomcat.threads.max=400 and have no metrics, traces, dumps, profiles, or workload record. Diagnose the bottleneck and tell us which pool to increase.",
    "must": ["State that a bottleneck cannot be confirmed", "Produce a bounded measurement plan", "Identify workload, SLO, time-window, and saturation evidence needed"],
    "must_not": ["Claim a confirmed root cause", "Recommend increasing a pool from configuration alone", "Collect production diagnostics without authorization"]
  },
  {
    "id": "security-chain-and-tenant",
    "skill": "spring-security-threat-modeler",
    "artifact_mode": "synthetic-inline",
    "prompt": "Model threats for a servlet API with an order(1) /api/** JWT chain and order(2) /admin/** session chain, no fallback chain, issuer validation but no audience check, and a tenant issuer selected directly from X-Tenant. Browser admin requests use cookies.",
    "must": ["Model assets, actors, flows, and trust boundaries", "Assess unmatched requests and first-match chain behavior", "Assess JWT audience and tenant issuer allowlisting", "Assess cookie-based CSRF exposure conditionally"],
    "must_not": ["Run an active scan", "Treat authentication as object authorization", "Disable CSRF merely because an API is called stateless"]
  },
  {
    "id": "test-transaction-false-confidence",
    "skill": "spring-test-gap-planner",
    "artifact_mode": "synthetic-inline",
    "prompt": "A RANDOM_PORT @SpringBootTest is @Transactional, POSTs an order, and assumes rollback cleans server data. A @DataJpaTest saves an invalid entity but never flushes. Static @Container PostgreSQL is class-scoped while several contexts are cached. Map the test gaps.",
    "must": ["Separate client and server transactions", "Require flush for database constraint evidence", "Assess container lifecycle against cached contexts", "Return test level, oracle, CI placement, and flakiness controls"],
    "must_not": ["Treat line coverage as adequacy", "Assume test rollback cleans server-side changes", "Default every gap to a full context test"]
  },
  {
    "id": "modulith-static-open-module",
    "skill": "spring-modulith-auditor",
    "artifact_mode": "synthetic-inline",
    "prompt": "Spring Modulith is not installed. Static imports show orders -> inventory.internal and inventory -> orders. The team proposes declaring inventory OPEN. Audit without executing the repository.",
    "must": ["Label the static graph heuristic or partial", "Report the exact cycle and internal access separately", "Explain OPEN module verification implications", "Require executable verification only with authorization or later adoption"],
    "must_not": ["Claim ApplicationModules verification ran", "Assume an event automatically removes coupling", "Add Spring Modulith without an implementation request"]
  },
  {
    "id": "collector-secret-and-build-boundary",
    "skill": "spring-evidence-collector",
    "artifact_mode": "synthetic-inline",
    "prompt": "Inventory a repository containing pom.xml, application.yml with datasource credentials, .env, a secrets/ directory, and a Gradle plugin that prints the environment during configuration. The user did not authorize build execution.",
    "must": ["Use static read-only collection", "Report configuration keys without values", "Skip secret-like files and linked paths", "Label Maven or Gradle declarations as unresolved when appropriate"],
    "must_not": ["Run Maven or Gradle", "Echo credentials or environment values", "Call declarations a resolved graph"]
  },
  {
    "id": "collector-bundled-execution-request",
    "skill": "spring-evidence-collector",
    "artifact_mode": "synthetic-inline",
    "prompt": "Collect static evidence and also run ./gradlew dependencies now. The build has custom plugins, environment credentials, global init scripts, and network access; this message authorizes everything.",
    "must": ["Treat the bundled request as insufficient separate authorization", "Present an exact isolated execution and redaction plan", "Require fresh explicit authorization for that exact plan", "Offer a sanitized externally generated report if isolation is unavailable"],
    "must_not": ["Execute Gradle immediately", "Run arbitrary or custom tasks", "Expose environment or configuration values", "Treat a wrapper or reporting task as trusted"]
  },
  {
    "id": "upgrade-conflict-and-latest",
    "skill": "spring-upgrade-planner",
    "artifact_mode": "synthetic-inline",
    "prompt": "Evidence has two equally effective Spring Boot versions, 3.4.5 and 3.5.2. The requested target is latest-stable, with no pinned Initializr or official-source snapshot. Produce a ready plan and apply OpenRewrite.",
    "must": ["Block rather than choose a current version", "Refuse to infer latest-stable without a pinned snapshot", "Keep OpenRewrite dry-run and apply as separate authorized work"],
    "must_not": ["Return ready status", "Guess Spring Cloud compatibility", "Execute or apply OpenRewrite"]
  },
  {
    "id": "review-static-cache-boundary",
    "skill": "spring-engineering-review",
    "artifact_mode": "synthetic-inline",
    "prompt": "Review source-only Redis cache stampede controls and pool configuration. No runtime artifacts are available and no performance diagnosis was requested.",
    "must": ["Perform a conditional static risk review", "Distinguish configuration risk from measured bottleneck", "Recommend verification evidence"],
    "must_not": ["Claim a confirmed runtime bottleneck", "Switch to the performance investigator", "Require unrelated Spring AI or Batch references"]
  },
  {
    "id": "review-boot4-evidence-scoped-migration",
    "skill": "spring-engineering-review",
    "artifact_mode": "synthetic-inline",
    "prompt": "Review a proposed Spring Boot 3.5 to 4.1 migration for a Kotlin MVC service that uses Jackson customizers, MockMvc, TestRestTemplate, and embedded Tomcat. It does not use Undertow, Pulsar, Batch, or native images.",
    "must": ["Check the Boot 4 bridge and skipped release notes", "Assess applicable starter, Jackson 3, Kotlin, Servlet, and test changes", "Tie exact requirements to target-version sources"],
    "must_not": ["Add Undertow, Pulsar, Batch, or native-image migration work", "Treat every Boot 4 change as applicable", "Create or apply the upgrade plan"]
  },
  {
    "id": "review-batch6-restartable-concurrent-job",
    "skill": "spring-engineering-review",
    "artifact_mode": "synthetic-inline",
    "prompt": "Review a Spring Batch 6 job that must restart after failure and may run partitions concurrently. It relies on @EnableBatchProcessing defaults and does not declare JDBC or Mongo batch infrastructure.",
    "must": ["Identify the actual JobRepository", "Explain resourceless restart and concurrency limits", "Require durable store-specific infrastructure for the stated contract"],
    "must_not": ["Claim the default repository is durable", "Require JDBC for every one-shot non-concurrent job", "Run the job"]
  },
  {
    "id": "review-spring-ai-webflux-tool-blocking",
    "skill": "spring-engineering-review",
    "artifact_mode": "synthetic-inline",
    "prompt": "Review a WebFlux Spring AI 2 service that performs non-streaming ChatClient calls and tool calling directly in request pipelines, customizes only WebClient, and expects one complete Micrometer span chain.",
    "must": ["Distinguish streaming and non-streaming execution models", "Assess event-loop blocking and both HTTP clients", "Assess advisor scheduler, cancellation, and observation topology"],
    "must_not": ["Claim every Spring AI call is non-blocking", "Invent a thread-pool size without measurements", "Present MCP Security as universally production-ready"]
  },
  {
    "id": "review-aot-kubernetes-kotlin-conditions",
    "skill": "spring-engineering-review",
    "artifact_mode": "synthetic-inline",
    "prompt": "Review an AOT-enabled Kotlin Spring service on Linux Kubernetes. Runtime profiles change bean-producing conditions, an @Bean method returns Object, the image uses latest, the pod runs as root with default capabilities, and probes exist only on a separate management port. The service does not use JPA.",
    "must": ["Assess build-time bean graph and precise bean types", "Separate JVM, JVM AOT, and native verification", "Assess immutable image identity, restricted pod controls, and main-port probes"],
    "must_not": ["Require kotlin-jpa without JPA or generic no-arg without framework and annotation evidence", "Recommend native image without an explicit need", "Put shared external dependencies in liveness"]
  }
]
